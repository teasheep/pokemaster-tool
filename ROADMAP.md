# 開發藍圖

**這不是願望清單，是「訊號 → 動作」對照表。** 沒有訊號就不做 —— 這個工具只有一個道館 20 個人在用，
提早蓋起來的東西維護成本是真的，而使用者反饋是免費的。

已完成的項目見 [CHANGELOG.md](CHANGELOG.md)。

---

## 進行中

| 項目 | 狀態 |
| --- | --- |
| 開放給第一個外部道館試用 | 需要先有隱私權政策（見下方「開放註冊前必須做的」） |

---

## 有明確訊號才做

排序依「訊號出現的可能性」，不是依難度。

### 資料與正確性

| 訊號 | 動作 |
| --- | --- |
| **每次新拍組上架日**（下一次 2026-09-12） | 重跑 `npm run data:catalog` 並推 tag 部署 —— 靜態圖鑑是建置時產的，不重跑看不到新拍組。`tests/catalog-asset.test.ts` 會在那天自己變紅提醒 |
| ~~下一場道館戰之前~~ | ~~單場看板的 `battle_logs` 查詢改分頁~~ **2026-09-03 已修**（連同 realtime 事件合併，見 CHANGELOG） |
| 賽事數 > 20 場 | 加聚合 RPC `gym_battle_totals`，賽事一覽只需要三個數字，現在卻把全部列拉進 Worker 再用 JS 數 |
| catalog 筆數破 800 | `user_collection` 的查詢改分頁（目前 664 筆，單人最大 130 列，還有餘裕） |

### 效能與成本

| 訊號 | 動作 |
| --- | --- |
| 頁面開始被抱怨慢 | 改寫 `member_pairs` 的 RLS policy。實測 2,055 列查詢 30.0 ms / 2,107 buffers → 改寫後 0.88 ms / 53 buffers（**34 倍**）。在 2 萬 MAU 時這一條值 92 美元/月 |
| 想再壓延遲 | 評估 Cloudflare Placement Hint `aws:ap-southeast-1`（正好是 Supabase 的區域）。**不要和其他網路層改動同時做**，會分不出是誰的功勞 |
| — | ~~接自訂網域改善 colo 路由~~ **已於 2026-09-02 實測否證**：接上網域後 colo 仍是 SJC，connect / TLS 與 workers.dev 在誤差內相同。426 ms 的地板另有原因，待重新調查 |

### 同時上線人數：誰會先撐不住

2026-09-03 盤過一次。結論是**看他們進哪一頁，差三個數量級** —— 所以「同時幾人會炸」不是一個數字。

| 情境 | 撐到哪 | 先壞的東西 |
| --- | --- | --- |
| 只是進首頁（訪客） | 幾千人同時進來都是非事件 | 不會壞，是**帳單**問題 |
| 登入後一般瀏覽 | 數十人同時活躍 | Supabase 那台實例變慢 |
| 道館戰看板開著 | 十幾人開始放大，200 人是硬牆 | 每次出刀的查詢放大 → **2026-09-03 已改善** |

首頁之所以便宜，是因為 `getSessionUser()` 在**沒有登入 cookie 時直接回 null**
（`src/lib/supabase/server.ts`），訪客的首頁完全不碰資料庫；圖片走 Workers Assets
（官方寫明 static assets 請求 free and unlimited）。實測一次冷訪問：手機 46 個請求 / 170 KB、
桌機 71 個 / 266 KB。動態渲染照 286 ms CPU 中位算，Workers Paid 含的 30M CPU-ms
≈ **每月 10 萬次頁面渲染**，超過之後大約每百萬次 6 美元。

| 訊號 | 動作 |
| --- | --- |
| 第二個道館開始用看板 | 確認 Supabase 方案。**Realtime 同時連線** Free 200 / Pro 500 條【外部數字會變】，每個開著看板的分頁佔 1 條 —— 這是第一道**硬牆**，不是變慢而是連不上 |
| 一場道館戰同時 30+ 人開看板 | realtime 改成**吃 `payload.new` 直接套用差異**，不要重抓整張表。合併窗口（2026-09-03 加的 200 ms）把「同一批動作」壓成一次查詢，但**人數的倍率還在**：N 個看板仍然是 N 次查詢 |
| 首頁月流量破 10 萬 | 開始有 CPU 帳單。要壓的話先讓首頁可快取（現在是 `force-dynamic`，因為要判斷登入與否），例如訪客版走靜態、登入者才走動態 |
| 帳單開始有感 | 先改 `member_pairs` 的 RLS policy（下一列，34 倍），**不要**先換託管平台 |

### 功能

| 訊號 | 動作 |
| --- | --- |
| 使用者提出 | **降抗（屬性抵抗）整套重做**。舊的分數試算 2026-08-16 整塊拔除，資料保留。官方用語是「屬性抵抗」，有階數／單體／全體／觸發之分，不要拿舊試算表的數值長回來 |
| 使用者提出 | **道館攻略與紀錄**重做。2026-08-17 暫時下架（只拔入口，頁面與資料表都還在） |
| 使用者提出 | **排刀**重做。schema 已於 0047 drop，重建時不要復活舊欄位名 |
| 要重啟拍照辨識 | 評估 Cloudflare Containers。Workers 跑不了 DINOv2 推論，但 Containers 在免費額度內邊際成本為零 —— 關鍵未知數是**休眠前的閒置時間**，那個參數決定整個估算成不成立 |
| 要重啟 EX 換裝立繪 | 三處要一起動：轉檔腳本的 TARGETS、upload 頁的路徑、`.assetsignore`。少一步就是線上 EX 立繪全 404（三處都已就地留註解） |

### 工程

| 訊號 | 動作 |
| --- | --- |
| 想上嚴格 CSP | 需要 middleware 逐次發 nonce。直接上 `script-src` 會整站白畫面（站上有三種 inline script：Next 的 flight 資料、next-themes 防閃爍、OpenNext 的 `__name` 補丁） |
| 有第二個貢獻者 | 補 E2E（目前只有單元測試 + 手動 render probe） |

---

## 開放註冊前必須做的

這一節是**法務不是工程**，而且是唯一一條「工程做得再好也擋不住」的風險。

第一個「不是朋友」的人註冊那一刻，個資法的個人／家庭活動豁免就消失了。在那之前需要：

- [ ] 隱私權政策（蒐集了什麼、存在哪、保留多久、怎麼刪除）
- [ ] 服務條款
- [ ] 帳號刪除流程（現在只能手動改資料庫）
- [ ] 確認 `/api/export` 與 `/share/<token>` 的資料範圍符合政策所寫

---

## 明確不做的事

寫在這裡是為了不用每次重想：

- **換掉 Cloudflare** —— 每個規模階段 Supabase 都是應用託管的 5–9 倍費用，換託管省的錢永遠小於 Supabase 升級一階
- **現在就重構成「可搬」** —— production 相依 0 個 Cloudflare 專屬套件，搬家成本 0.5–1 人日**且與規模無關**。晚點搬不會變貴，提早抽象只會變複雜
- **長出第四個分頁** —— 資訊架構是使用者指定的，加東西前先問「現有哪一頁的職責涵蓋它」
- **同一件事開第二個入口** —— 有前科：隊伍庫曾被收進看板側板，結果分頁消失、看板裡的連結指向轉導頁
