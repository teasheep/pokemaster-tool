# 開發藍圖

接下來想做什麼、哪些地方還可以做得更好。已經做完的見 [CHANGELOG.md](CHANGELOG.md)。

這個工具現在只有一個道館 20 個人在用，所以排序看的是「有沒有人真的需要」，不是難度。
沒有人開口要的東西不會因為「聽起來很酷」就先蓋起來。

---

## 進行中

| 項目 | 說明 |
| --- | --- |
| **開放給第一個外部道館試用** | 卡在隱私權政策與帳號刪除流程（見下方「開放註冊前必須做的」），不是卡在功能 |

---

## 要做，等時機

這幾個都是**功能還在、只是先收起來**的：頁面與資料表都留著，重做時不用從零開始。

### 屬性抵抗（降抗）

整套重做。舊版是從試算表匯入的分數，遊戲裡沒有對應的東西，2026-08-16 整塊拔除（資料保留）。

重做時要照遊戲真正的制度走 —— 官方用語是「屬性抵抗」，有階數、單體／全體、觸發條件之分，
**不要**拿舊試算表的數值長回來。連帶會回來的還有排刀的降抗先手與「全體屬性戰力」面板。

### 道館攻略與紀錄

2026-08-17 暫時下架，只拔了入口，頁面程式與資料表都還在。要重做的是「一場道館戰該怎麼打」
這件事本身該長什麼樣，不是把舊版接回來。

### 排刀（誰去打哪一關）

schema 已於 0047 移除。演算法還在（`lib/gym/assign.ts`，有測試），但沒有任何入口。
重建時 schema 要重新設計，不要復活舊欄位名。

### EX 換裝立繪

資料欄位與圖檔都保留著，只有 UI 拔掉了（呈現方式不對）。重啟時有三個地方要一起動，
程式裡三處都留了註解。

### 拍照辨識拍組

這是這個專案最初的目標 —— 上傳遊戲截圖自動認出是哪些拍組（DINOv2 embedding 兩階段比對），
2026-08 中轉向道館賽功能後暫緩，管線程式碼完整保留。

要重啟的前提是找到能跑推論的地方：Workers 跑不了，Cloudflare Containers 是目前最合適的候選。

---

## 可以再精進的

沒有壞，但有更好的做法。

| 項目 | 現況 → 想要的樣子 |
| --- | --- |
| **道館戰看板的即時更新** | 有人出刀時，看板是重抓整張表；改成直接套用變更的那一列會更省、也更即時。已先做事件合併（2026-09-03） |
| **賽事一覽的統計** | 現在把所有紀錄拉回來用 JS 數，其實只需要三個數字 —— 交給資料庫算 |
| **等待畫面** | 骨架已經與真實版面同構，但還有幾頁沒補上 |
| **E2E 測試** | 目前只有單元測試 + 手動的 render probe，關鍵流程（登入 → 建館 → 出刀）值得自動化 |
| **更嚴格的 CSP** | 現在只上了 `frame-ancestors` / `base-uri` / `form-action` / `object-src`。要上 `script-src` 需要逐次發 nonce，站上有三種 inline script |

### 每次新拍組上架都要做的

不是功能，但會定期回來：拍組上架日（下一次 **2026-09-12**）要重跑 `npm run data:catalog`
並推 tag 部署 —— 圖鑑的靜態檔是建置時產的，不重跑線上看不到新拍組。
`tests/catalog-asset.test.ts` 會在那天自己變紅提醒。

---

## 開放註冊前必須做的

這一節是**法務不是工程**，而且是唯一一條「工程做得再好也擋不住」的風險。

第一個「不是朋友」的人註冊那一刻，個資法的個人／家庭活動豁免就消失了。在那之前需要：

- [ ] 隱私權政策（蒐集了什麼、存在哪、保留多久、怎麼刪除）
- [ ] 服務條款
- [ ] 帳號刪除流程（現在只能手動改資料庫）
- [ ] 確認 `/api/export` 與 `/share/<token>` 的資料範圍符合政策所寫

---

## 明確不做的事

寫在這裡是為了不用每次重想：

- **長出第四個分頁** —— 資訊架構是使用者指定的（拍組／道館 + 我的資源），
  加東西前先問「現有哪一頁的職責涵蓋它」
- **同一件事開第二個入口** —— 有前科：隊伍庫曾被收進看板側板，結果分頁消失、
  看板裡的連結指向轉導頁
- **為了「之後可能要搬」先做抽象層** —— production 相依 0 個平台專屬套件，
  真要搬的成本與規模無關。晚點搬不會變貴，提早抽象只會變複雜
- **把匯入來源的結構長成功能** —— 匯入的表單只取「誰有什麼、練度多少」，
  它的分類／標籤／權重一律丟棄
