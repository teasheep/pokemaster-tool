# 更新日誌

本檔遵循 [Keep a Changelog](https://keepachangelog.com/zh-TW/1.1.0/)，版本號採 [語意化版本](https://semver.org/lang/zh-TW/)。

發布方式：推一個 `v*` tag，GitHub Actions 才會部署到線上。push 到 `main` 只跑檢查。

## [未發布]

預計要做的事見 [ROADMAP.md](ROADMAP.md)。

## [1.5.0] — 2026-09-08

站徽、法遵頁與頁尾 —— 都是「站要能對外交代自己是什麼」的部分。

### 新增

- **隱私權政策 [`/privacy`](https://pokemaster-tool.com/privacy) 與服務條款
  [`/terms`](https://pokemaster-tool.com/terms)**。內容是照這個站**實際上**存了什麼、
  用了哪些外部服務寫的（Supabase 存資料、Cloudflare 跑網站、Google 負責登入），
  不是套來的範本 —— 例如站上真的沒有裝任何分析或追蹤工具，所以就這樣寫。
  兩頁都不需要登入就打得開，頁尾每一頁都連得到。
  （Google 登入的審核要求要有這兩個公開網址。）
- **要求刪除帳號與資料的聯絡方式**：寫在兩頁的「聯絡方式」，走作者的信箱。
  請用你登入這個站的那個 Google 帳號寄信 —— 這是我們唯一能確認「是本人提的」的方法。
  功能建議與問題回報則到 GitHub 開 issue（**issue 是公開的**，別在裡面留個人資料）。
- **站徽**：黑白的大師球外框，球裡寫「教練」。分頁上的小圖示（favicon）與站上左上角
  都換成它了 —— 原本那顆沙發是暫代的。

### 變更

- **頁尾重做**。收尾的那條線是大師球的接縫，往兩端淡出。
  底下只留三個入口：隱私權政策、服務條款，以及一顆連到原始碼的 GitHub 圖示。

## [1.4.2] — 2026-09-08

開放給第一批測試者前的除錯。

### 修正

- **在道館改拍組時不會再弄丟等級與 6★EX**。原本把某張卡改成「未持有」再改回寶數，
  會把那位成員的等級與星數洗掉，而且全程沒有任何錯誤訊息。
- **第一次登入時，教學跳出來的當下就會說明「操作不會存進資料庫」**。
  原本那句話要等你點進步驟才出現，所以新成員看到教學卡、不理它、
  直接去按「用邀請碼加入」，只會得到一句紅字失敗，完全不知道是教學擋的。
- **錯誤頁的「重試」按鈕真的會動了**。出事時唯一的出口，自己壞著。
- **切到「所有遊戲拍組」後重新整理，還是完整的圖鑑**。
  原本會變成一份只有一百多隻的清單，沒有載入中也沒有錯誤，
  你會以為站上根本沒收錄那隻拍組。
- **沒登入的人開到帶「只看我持有的」的網址，不會再看到一片空白**（而且救不回來）。
- **管理員設好「道館拍組」★ 之後切分頁再回來，★ 不會消失**。
  原本看起來像沒存到，再按一次反而是把它移出名單。
- **顧問（唯讀身分）不會再看到按下去必定失敗的「新增紀錄」表單**。
- **教學在手機上不會卡住**：手機的成員名冊是收起來的，教學會告訴你先打開它；
  另外「已經在道館列表卻又叫你點道館」的原地繞圈也修掉了。

### 開發工具（線上沒有這些）

- **訓練家立繪取景工具** `/dev/trainer-art`：拉滑桿調遠近／左右／上下，
  旁邊就是真正的卡片與原生縮圖基準，滿意再按「寫入」。
- **`npm run qa:click`**：用真的 Chrome 把主要流程點過一遍（19 條），
  並把前端未捕捉的錯誤一律當失敗。這一輪的 bug 有一半是點下去才會發生的。

## [1.4.1] — 2026-09-08

重新整理留在原本的畫面；九位訓練家的立繪取景校正。

### 修正

- **重新整理會留在你目前的畫面，不再跳回「道館重點拍組」**。
  現在這些狀態都記在網址裡：拍組頁的子分頁與「只看我持有的」、
  道館成員頁的「在看誰／拍組或資源分頁／範圍／只看持有的」。
  順帶的好處是**上一頁／下一頁會照你預期走**，而且把網址貼給別人，對方看到的就是你看到的那一頁。
- **9 位訓練家的立繪取景跟其他拍組對齊了**（第二版，第一版的頭太大、人偏左）。他們的圖是全身照，縮進卡片的框裡就變成
  「小人漂浮」，跟旁邊的頭肩胸像放在一起很突兀
  （使用者回報：伊芙（寶可夢小朋友）、所有主角拍組、華蓮（長袖和服少女））。
  全部改從原始的高解析立繪重新裁成胸像。
  除了回報的那幾隻，掃過全部 471 張後補上了同樣有問題但沒被列到的：
  丘助（寶可夢小朋友）、勇仁（登山男）、汐音／亞莎美／桐華（長袖和服少女）、紗幽子（靈異迷）。
  主角那張影響最大 —— 11 個拍組共用它。
  新增測試會在以後又混進全身圖時擋下來（這種問題沒有徵兆，只是某幾張卡上的人比較小）。

## [1.4.0] — 2026-09-07

拍組側板統一，道館也能管星數與等級。

### 新增

- **道館看得到、也改得動成員的星數與等級**（點進拍組卡片，跟看自己的一樣要點進去）。
  以前這兩個值道館這邊根本沒有，現在會跟著你在「拍組」頁的設定同步過去。
  點卡片左下角調寶數**不會**動到星數與等級 —— 那個手勢管的是寶數，
  不該順手把人家設好的 6★EX 或 Lv200 改掉。
  **卡牆上的星星不受影響**：道館的拍組牆照舊一律顯示拍組的原始星級，
  個人升到幾星只在點開的側板裡看得到。

### 變更

- **等級改成 1 / 140 / 150 / 180 / 200 五個值**，其中 **Lv1 = 還沒設定**。
  原本不在這幾個值裡的資料（線上有兩筆 Lv100、Lv130）已經一併收成 Lv1。
  新點亮一隻拍組的預設也是 Lv1 —— 之前是 Lv200，但系統其他地方建立的資料都是 Lv1，
  兩套預設對不起來，才會出現那 167 筆看起來壞掉的 Lv1（其實只是沒設定過）。

### 修正

- **道館裡點開拍組，現在跟「我的拍組」是同一個側板了**。原本道館那邊只能看，
  寶數要改得去點卡片左下角 —— 同一張卡、同一個側板，左下角改得動、下拉卻沒有，
  沒有道理。現在兩邊是同一顆面板、同樣的欄位，下拉與左下角都能用。
  沒有權限改的人（顧問、看別人的一般成員）看得到同一份版面，只是動不了。
- **管理員幫「還沒建立過收藏紀錄」的成員點亮拍組時，星數不再被記成 5★**
  （不管那隻拍組本來是幾星）。現在記的是正確的原始星級。
- **在道館改別人的練度，畫面立刻就變**（以前要等伺服器回來才動，下拉會先彈回舊值）。
- 「設為道館拍組」那顆按鈕的文字，兩個側板現在完全一樣（有一邊之前寫的是「取消道館拍組」）。

## [1.3.0] — 2026-09-07

使用教學、賽事模板、「我的資源」屬性方向、換頁提速。

### 新增

- **使用教學**。分兩條路：「我是道館負責人 → 我要建立道館」與
  「我是成員 → 我要管理拍組資訊」，走完再問要不要接著看「道館戰怎麼跑」（可以不看）。
  **是跟著點，不是看投影片**：教學不會替你換頁，是框住該點的東西等你自己點 ——
  自己點進拍組頁、自己點開側板、自己點卡片左下角調寶數。點對了框會閃一下綠色再往下走，
  每一步都留一顆「跳過這步」，不會把人卡住。
  **教學期間的任何操作都不會存進資料庫**——連「建立賽事」也一樣，教學不留任何資料。
  頂端一直有一顆膠囊寫明這件事；離開教學時如果動過東西，會重新載入把畫面拉回你真正的資料。
  不在那一頁的時候**教學會指路**：框住「進去那一頁的入口」（桌機頂端那排、手機底部那排、
  道館裡的分頁、或道館切換器裡的「加入 / 建立道館」），換上那個入口的說明，等你自己點 ——
  走到了才換回原本那一步。不會替你開，因為記得路怎麼走才是重點。
  整層不吃點擊 —— 壓暗只是視覺，教學開著的時候畫面照樣操作得動。
  **第一次登入落地時自己跳一次**，之後在右上角頭像選單的「使用教學」隨時能再叫出來。
  多一步教「怎麼看館內其他人的拍組」（道館 → 成員與拍組 → 點成員）。
  講的是「不講就沒人會發現」的那幾件事：點卡片左下角可以直接循環寶數、顧問碼不佔 20 人名額、
  賽事狀態是由賽期日期推導的。
  手機不是另一套版面：卡片吃滿螢幕寬、自動讓開底部導覽列，而同一個目標
  （例如「拍組」分頁）在桌機是 header、在手機是底部那一列 —— 教學只框看得見的那一個。
  還沒有道館的人跑「我要建立道館」時，後面幾步要框的東西本來就不存在 ——
  那幾步會降級成置中的說明卡，內容照樣看得完，不會卡住。
  不會乾等：**下一頁在你讀這一步的時候就先抓好了**，而框與說明卡的位置每一幀直接寫進 DOM
  （不經 React），所以跟著捲動時是黏著元素走的，不是追在後面。
- **「我的資源」不只是糖果了**：除了糖果庫存，多兩塊可以複選 18 屬性的板塊 ——
  **想投入資源的屬性**與**已投入較多資源的屬性**，標題後面用灰色小字標出「資源」指什麼
  （裝備、突破等級、潛能）。
  兩塊不互斥：已經練得深、還想再練是常態。
  同一組板塊也出現在道館的成員頁（「糖果」分頁改名「資源」），所以安排道館戰的人看得到
  誰在練哪一路；看別人時只列出他選的屬性，不畫 18 格灰卡。
  `/api/export` 一併帶出（`typeFocus`），資料連線那頁的回傳結構也補上了。
- **建立賽事可以套用模板**。遊戲每一回帕希歐道館對戰的 8 關弱點屬性是固定的，
  所以「第一次（集結關都館主）／第二次（集結伽勒爾館主）／第三次（城都館主大集合）」做成**系統層級**的模板 —— 選一個，8 關屬性一次填好，
  不用建立完再一格一格點 8 次。選之前就看得到那個模板會填哪 8 個屬性。
  「先不套用」永遠留著（遊戲開了新的一回而模板還沒補上時走這條）。
- **`/resources` 補上載入骨架**。`force-dynamic` 的路由沒有 `loading.tsx` 就不會被
  prefetch —— 少了它，從導覽列按過去是整頁乾等伺服器回應、畫面零反應。
  現在骨架立刻出現，而且這條路由才進得了預抓。

### 修正

- **教學期間「建立道館／加入道館」不再假裝成功**。這兩個動作會拿回傳的道館編號直接導頁，
  被吞掉之後假成功等於把人導去一個壞掉的網址；現在跟「建立賽事」一樣，會直接告訴你
  「使用教學進行中 — 這個操作不會存進資料庫」。換頭貼（上傳到 Storage）同理 ——
  假成功會顯示一張根本沒傳上去的破圖。
- **教學框住的東西中途消失時（例如你把側板關掉了），框不再塌到畫面左上角**。
  現在會退回置中的說明卡繼續講，你再打開它就接得回來。

- **成員的「道館拍組」分頁不再混進他自己持有的其他拍組**。原本那個分頁是
  「道館名單 ∪ 這個人持有的全部」，所以沒被設為道館拍組的卡也會出現在裡面 ——
  而同一頁的「全館拍組 → 道館拍組」卻是嚴格的，同一個分頁名兩種意思。現在兩邊一致。
  他持有、但不在名單裡的拍組改到「所有遊戲拍組」看，並且那裡補上了
  **「顯示全部 / 只看持有的」**開關（跟 `/pairs` 同一顆藥丸、同一個心智模型）。

### 效能

- **已登入者的每一次換頁少掉一趟跨太平洋的驗證往返**。middleware 原本每個請求都問一次
  Supabase「這個人是誰」，只為了決定要不要導去登入頁；實測那一趟 77–973 毫秒，
  而頁面自己的工作常常只有 63 毫秒 —— 整頁時間九成花在那個決定上。
  現在只有「快到期／已過期／cookie 讀不出來／登入註冊頁」才問，其餘直接放行（3–4 毫秒）。
  真正的權威判斷本來就在每一頁自己的檢查上，那裡沒有改。
  驗過六條：登入進得去、偽造 cookie 被擋下、已登入開登入頁會導去道館、
  過期的 cookie 會換到新的、登出後被導回登入頁、訪客導去登入頁。

---

## [1.2.0] — 2026-09-04

首頁重做、基本 SEO、看板拿掉即時推播。

> 這是**第一個透過 tag 部署的版本**。1.1.0 以前是在本機 `npm run deploy:cf` 推的，
> 所以那幾版在這個版本庫裡沒有對應的 git tag。

### 新增

- **首頁 hero 改成兩塊看板輪替**：全館拍組持有（20 人中幾人有）與道館戰（誰還剩幾張券），
  正好對上標題那句的兩件事。兩塊一直都掛著、只是輪流交叉淡入，所以切換時版面不動；
  點圓點就停止自動輪替。`prefers-reduced-motion` 照樣輪，只是換場不做位移 ——
  那個偏好要擋的是會動的東西，換一塊內容本身不是動態，整個停掉等於讓人少看到一半的內容。
- **兩塊看板各自演一段真正的操作**（不是靜態插圖）：
  拍組是「灰卡 → 點左下角 → 寶1/2/3，卡片亮起、全館持有跟著 +1」（`syncMemberPair` 那條雙表同步）；
  道館戰是「選一組拍組 → 回報 → 那個人的挑戰券 13→12 並亮一下」（`reportBattleLog`）。
  用的是站內真的 `SyncPairCard` 與真的手勢，不是另外畫的示意圖。
- **首頁看得出是哪一款遊戲**：看板每一列的圖是**一組拍組**（訓練家立繪當本體、寶可夢壓右下角，
  官方卡面的排法），示範成員用他們自己的訓練家立繪當頭貼。
  背後再擺七張**真的 `SyncPairCard`**，各自有遠近（scale／透明度／景深模糊）、各自慢慢浮動，
  **滑鼠一動整個空間跟著微微轉** —— 近的卡位移大、遠的小，那個差就是空間感。
  中央挖空，字與 CTA 上面永遠不會有東西。手機/觸控整層不掛載（一張圖都不會下載）。
  只吃既有的 WebP：手機 14 張 / 75KB，桌機 42 張 / 179KB。
  `prefers-reduced-motion` 分兩種處理：自己會動的（呼吸、滑入的位移）關掉或換成純淡入，
  但**跟著指標走的視差照做、幅度調弱** —— 那是直接操作不是自動播放，規範叫 reduced 不是 removed。
- **首頁拍組看板列的是最新上架的五組 5★**（吃 catalog，會自己跟著改版更新），持有人數隨機。
  4★ 不列 —— 每一波改版 5★ 與 4★ 一起上，不濾的話首頁一半是 4★。
- **Google One Tap 改成沒登入時全站都有**（`GoogleOneTapSlot` 掛在 root layout）。
  已登入者整個不渲染，連 GIS script 都不載。代價是知情接受的：每頁都掛等於每頁都把
  「有人來過」告訴 Google；而使用者關掉 One Tap 會讓瀏覽器對整站進入 FedCM embargo。
- **基本 SEO**。`robots.txt` 與 `sitemap.xml`（`app/robots.ts` / `app/sitemap.ts`）、
  canonical、Open Graph／Twitter card、分享卡片圖 `public/og.png`（1200×630）、
  首頁的 `WebApplication` 結構化資料。站台常數集中在 `lib/site.ts`，網域只有一份。
- **全站只有 `/` 與 `/pairs` 可以被收錄**。`robots.txt` 與 `sitemap.xml`（`app/robots.ts` / `app/sitemap.ts`）、
  canonical、Open Graph／Twitter card、分享卡片圖 `public/og.png`（1200×630）、
  首頁的 `WebApplication` 結構化資料。站台常數集中在 `lib/site.ts`，網域只有一份。
- **全站只有 `/` 與 `/pairs` 可以被收錄**。其餘（道館、個人設定、onboarding、登入流程）
  一律 `noindex`；**`/share/<token>` 尤其重要** —— token 是半秘密，被搜尋引擎收錄
  等於把成員的收藏攤在搜尋結果上，所以 `noindex` 與 robots 的 `Disallow` 兩層都下。
  `tests/seo.test.ts` 釘住這幾條（它們壞掉不會有任何徵兆）。

### 變更

- **右上角的「登入」鈕在首頁與登入頁不再渲染**：這兩頁畫面裡已經有一顆真的 Google 按鈕，
  再放一顆等於同一個動作兩個入口、而且兩顆長得不一樣。其他訪客看得到的頁（`/pairs`、分享頁）
  一定要留著 —— One Tap 偵測不到有沒有跳出來，那顆是唯一的保底入口。
- `docs/` 改為**本機筆記，不進版控**（決策脈絡、資料管線操作、降抗查證）。
  檔案仍在本機，AGENTS.md 與程式註解照樣指得到；README／ROADMAP 的連結已改掉。
- **道館戰看板拿掉 Supabase Realtime**（`0054` 加了 broadcast，`0055` 又整套撤掉；
  兩個都沒上線過）。改成兩件無聊但夠用的事：**回到分頁就重抓**（visibilitychange + focus），
  以及**賽事進行中、分頁看得見時每 45 秒抓一次出戰紀錄與券數**。已結束的賽事兩件都不做。

  拆掉的理由是量出來的：線上 `battle_logs` 的 291 筆**全部**寫在 13 秒內
  （2026-08-13 06:19:53–06:20:06），那是試算表匯入不是有人在看板上回報；真正透過 UI
  回報過的次數，看 `gym_activity` 的 `battle_log` 只有 **5 次**、橫跨 3 天。
  即時推播推給了零個觀眾，換來的卻是一條硬性連線上限（全站唯一的擴展硬牆）、
  一個掛在**出刀回報**這條關鍵寫入路徑上的 trigger，以及全站唯一無法在正式環境驗證的路徑。

  加回來的判準寫在 [ROADMAP.md](ROADMAP.md)：先看下一場真正的道館戰之後，
  出刀是不是成群出現。`0054` 留在版本庫裡，要加回來原封不動重跑即可。

### 修正

- **`robots.txt` 與 `sitemap.xml` 被登入牆導去 `/login`**。它們是 `app/robots.ts` /
  `app/sitemap.ts` 產的**路由**不是靜態檔，middleware matcher 的副檔名規則擋不到 ——
  爬蟲拿到的是 `302 → /login?redirect=%2Frobots.txt`。（加 SEO 的同一輪實測抓到。）
- **看板的 `battle_logs` 重抓沒有分頁**（會靜默截斷）。首次 SSR 是分頁的，但畫面上的重抓
  走的是另一支純 `.eq()` 的查詢 —— 一旦單場超過 1000 列，看板會**無聲**停在剛好 1000。
  AGENTS.md 列了三個必須分頁的地方，這是漏掉的第四個。順帶給 `fetchAllRows` 加了
  `firstBatchPages` 選項：預設的「一批 3 頁」是拿 2 個空請求換一趟跨太平洋往返，
  但這支會被 N 個瀏覽器同時呼叫，那個交換要反過來算。
  `tests/fetch-all.test.ts` 釘住兩種行為。

---

## [1.1.0] — 2026-09-02

網域搬遷、Google 登入合規化、首頁重做。

### 新增

- **自訂網域 `pokemaster-tool.com`** 取代 `*.workers.dev`。Worker Custom Domain 由
  `wrangler.jsonc` 宣告（`custom_domain: true`），同時 `workers_dev: false` 關掉舊入口。
- **Google One Tap** 登入（`signInWithIdToken`），掛在 `/login`。
  GIS script 的載入本身就是一個帶 Google cookie 的請求，所以當時只掛需要登入的那一頁。
- **首頁的道館戰看板縮影** — 取代原本三段功能介紹文字。四個示範成員 + 一筆剛登記的出刀，
  陌生道館的人瞄一眼就有概念，不用讀說明。
- 登入後導向的閘門抽成 `lib/auth/post-login-destination.ts`，由 `/auth/callback` 與
  新的 `/auth/one-tap` Route Handler 共用。

### 變更

- **登入按鈕改為符合 Google 官方品牌規範**：官方漸層版 super G（取自官方素材包）、
  Google Sans Medium（`next/font/google` 自我託管）、官方尺寸與雙主題配色、
  官方繁中字串「使用 Google 帳戶登入」。規範原文寫明「遵守本指南是 app verification 的條件」。
  刻意**不用** `google.accounts.id.renderButton()` —— 它是 `accounts.google.com` 的 iframe，
  GIS 被擋（Brave Shields、擋腳本的擴充）時整個登入入口會消失。
- **首頁只服務訪客**：已登入者一律直接進 `/gyms`（那頁本來就有「建立／加入道館」的空狀態），
  順帶省掉一次跨太平洋的 gyms count 查詢。CTA 從兩顆（都連 `/login`）收成一顆。
- 手機版 header 只剩「品牌｜頭像」，主題切換移進頭像選單。
- 已登入者打 `/login` 的落點由 `/pairs` 改為 `/gyms`，與 `safeNextPath` 的預設值對齊。

### 修正

- **未公布拍組外洩**（安全性）。Fandom roster 會收錄 datamine 先行列，reconcile 因此給了它們
  `verifiedSources: ["wiki"]`，原本「無來源且無上架日」的判準一筆都擋不到 ——
  7 筆官方尚未公布的拍組進了對外的 catalog 靜態檔，而該檔由 Workers Assets 直送、
  訪客不用登入就抓得到。判準補上第二條：上架日晚於今天（台北）也擋。
- wiki 上架日的比對改走 `pomaPairId` by-id join。原本純名稱比對對不上帶形態後綴的列
  （wiki 的 `Palkia (Origin Forme)` vs datamine 的 `Palkia`），6 筆拍組因此沒有上架日。
- 4 筆已上市的大師／EX 大師拍組因 pomatools 未收錄而 `pairKind = "none"`，
  卡片沒有大師徽章、系列篩選按哪個 chip 都找不到。
- `update-catalog` 的變動追蹤欄位從 13 個補到 20 個。`releaseDate` / `series` / `pairKind` /
  `hasAwakening` 原本都不在名單裡，那幾欄的靜默退化永遠不會被印進報告。

### 資料

- 新增 19 組拍組（含銀河隊三幹部、2026 週年慶青綠／莎莉娜、冠軍小光／明輝）。
- 補收「主角 & 毒貝比」。主角拍組清單是手寫的，每年 8/28 多一隻就會落後；
  現在會拿 datamine 的數量對照並警告。

---

## [1.0.0] — 2026-08-21

第一個穩定版本：資訊架構收斂完成，安全性與效能的實測整批處理完。

### 新增

- **道館戰看板**：每關每輪的出刀登記（主力／補刀／降抗），自動扣挑戰券。
- **挑戰券**「剩餘 / 上限」制（預設 30/30），寫入一律走 `adjust_member_ticket` 原子 RPC。
- **賽事狀態由賽期日期推導**（台北時區），沒有手動狀態下拉。
- **隊伍庫**：每個屬性用哪三隻，看板選隊吃同一份。
- **資料連線** `/api/export`：唯讀金鑰跟著人走，範圍是這個人加入的每一個道館 + 自己的收藏。
- **分享連結** `/share/<token>`：把收藏分享給沒有帳號的人。

### 變更

- **資訊架構收斂**成頂部兩個分頁（拍組／道館）+ 我的資源；道館底下三個分頁。
  移除沒有分頁的「道館總覽」孤兒頁，內容拆回各頁。
- **權限模型**改為 管理員／成員／顧問，移除「館主」概念（`gyms.owner_id` 已刪）。
  最後一位管理員動不了（DB trigger 保護）。
- **練度欄位只留遊戲裡真的有的**：寶數與超覺醒合併成 0–10 的單一軸。
- 加入道館只填邀請碼，名字跟人走（存在 profiles）。

### 效能

- 「全圖鑑」從每次重算的 API route 改成指紋化的靜態資產，該路徑變成 0 CPU
  （Workers Assets 直送，Worker 不執行）。
- 卡片重圖延遲載入：一個 root 共用一顆 IntersectionObserver。
  /pairs 全圖鑑實測 981 個請求 / 14.7 MB → 桌機 208 個 / 2.75 MB、手機 93 個 / 1.04 MB。
- 圖片全面轉 WebP，部署 payload 6573 檔 / 109.68 MB → 1053 檔 / 5.08 MB。
- 等待畫面全部改成與真實版面同構的骨架。

### 安全性

- 收回 `log_activity` 等 `security definer` 函式的公開 EXECUTE。
  Postgres 對函式的 EXECUTE 預設 grant 給 PUBLIC，而 Supabase 另外明授給 `anon`/`authenticated`
  —— 只寫 `revoke ... from public` 是無效的。
- 修掉 PostgREST「一次最多 1000 列」的三處靜默截斷（跨賽事查詢、單場對戰紀錄、`/api/export`）。
  實測推算：20 人辦到第 4 場就會少算約 330 張券，而且不會報錯。
- 安全標頭改寫在 `next.config.ts` 的 `headers()`（`public/_headers` 只作用在靜態資產命中的回應）。

---

## [0.1.0] — 2026-08-11

初版。原本的目標是「上傳遊戲截圖自動辨識拍組」（DINOv2 embedding 兩階段比對），
2026-08 中轉向道館賽協作功能，辨識管線暫緩但程式碼保留。

[未發布]: https://github.com/teasheep/pokemaster-tool/compare/v1.4.2...HEAD
[1.4.2]: https://github.com/teasheep/pokemaster-tool/compare/v1.4.1...v1.4.2
[1.4.1]: https://github.com/teasheep/pokemaster-tool/compare/v1.4.0...v1.4.1
[1.4.0]: https://github.com/teasheep/pokemaster-tool/compare/v1.3.0...v1.4.0
[1.3.0]: https://github.com/teasheep/pokemaster-tool/compare/v1.2.0...v1.3.0
[1.2.0]: https://github.com/teasheep/pokemaster-tool/releases/tag/v1.2.0

<!-- 1.1.0 以前是在本機 `npm run deploy:cf` 部署的，這個版本庫裡沒有對應的 tag，
     所以不放比較連結（連過去會 404）。 -->

